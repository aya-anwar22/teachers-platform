import { BookingTransformInterceptor } from './booking-transform.interceptor';

describe('BookingTransformInterceptor', () => {
  it('should be defined', () => {
    expect(new BookingTransformInterceptor()).toBeDefined();
  });
});
